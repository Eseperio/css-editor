# css-editor
A tool for editing css over an existing html page. Pick an element in a page and it will automatically generate the most unique selector, then, an editor with all css options available will be rendered as side panel. It will generate all css for the requested changes
